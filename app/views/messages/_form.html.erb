<%= simple_form_for :message, url: [:reply_conversation] do |f| %>

	<%= f.input :body, error: false, label: false, placeholder: 'Reply to conversation...', input_html: { class: 'forms', rows: '1' } %>
	<%= f.submit "Reply", class: 'btn btn-success' %>

<% end %>