$("#update_<%= @updateable.id %>").html("<%= escape_javascript(render :partial => 'shared/updates', :collection => @updates, :as => :update) %>");
$('#update').modal('hide');
$('.redactor_editor').html('');
$('#update_form')[0].reset();
