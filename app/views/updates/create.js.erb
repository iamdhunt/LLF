$("#update_<%= @updateable.id %>").prepend("<%= escape_javascript(render :partial => 'shared/updates', :locals => {:update => @update}) %>");
$('#ut_pe_ct').html("<%= @updateable.updates.count %>");
$('#update').modal('hide');
$('#update_form')[0].reset();
