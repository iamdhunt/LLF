<% publish_to "/conversations/#{conversation.id}" do %>
	$("#messages_<%= conversation.id %>").prepend("<%= escape_javascript(render :partial => @receipt, :locals => {:receipt => @receipt}) %>");
<% end %>
$('#reply_form')[0].reset();
