<span class="btn-group">
	<%= link_to "/page.html", :class => "dropdown-toggle", :data => {:toggle => "dropdown", :target=>"#"}, title: "Options", alt: "Options", :class => "" do %>
    	<i class="fa fa-sliders ims-act more_e_p"></i>
	<% end %>
    <ul class="dropdown-menu">            
      <li>
        <%= link_to "Edit", edit_event_path(event), class: '' %>
      </li>
      <li class="del">
        <%= link_to "Delete", event_path(event.id), remote: true, method: :delete, data: { confirm: 'Are you sure you want to delete?' } %>
      </li>
    </ul>
</span>